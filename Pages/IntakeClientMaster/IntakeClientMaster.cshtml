@page
@model TURN.Pages.IntakeClientMaster.IntakeClientMasterModel
@{
	Layout = "_LayoutLatest";
}

@Html.Partial("_Navbar")

<style>
   

    h2 {
        text-align: center;
        background-color: #007bff;
        color: white;
        padding: 10px;
        border-radius: 5px;
    }

    /* Label and input alignment */
    .form-row {
        display: flex;
        align-items: center;
        margin-bottom: 15px;
    }

        .form-row label {
            width: 200px;
            font-size: 1rem;
            color: #333;
        }

        .form-row input,
        .form-row select {
            flex: 1;
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 1rem;
        }

    /* Mandatory field styling */
    label.required::after {
        content: " *";
        color: red;
    }

    /* Button group */
    .btn-group {
        display: flex;
        justify-content: flex-start;
        margin-top: 20px;
    }

    .btn-custom {
        margin-right: 10px;
        padding: 10px 20px;
        border-radius: 5px;
    }

    /* Styling for the background and content */
    .form-container {
        background-color: #e3f2fd; /* Light blue background */
        border-radius: 10px;
        padding-top:10px;
    }

        .form-container label {
            font-size: 1.2rem;
            font-weight: normal; /* Ensure label is not bold */
            padding-left: 0.5rem;
            margin-left: 15px; /* Adds space before labels */
        }

            /* Adding red color to mandatory fields */
            .form-container label.required::after {
                content: " *";
                color: red;
            }

        .form-container .form-control {
            font-size: 1rem;
        }

    h2.highlight-header {
        background-color: royalblue;
        color: white;
        padding: 5px;
        border-radius: 8px;
        text-align: center;
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        margin-bottom: 40px;
    }

    /* Add space above form fields */
    .form-container .row {
        display: flex;
        align-items: center;
        margin-top: 15px; /* Space above each form field */
    }

    /* Add margin below the form */
    .container.mt-8 {
        margin-top: 120px;
    }

    /* Ensure the label and input stay on the same line */
    .form-container .col-sm-6.d-flex.align-items-center {
        display: flex;
        align-items: center;
    }

    /* Reduce the input width */
    .form-container input[type="number"] {
        width: auto;
    }

    
</style>

<div class="container mt-8 form-container">
    <h2 class="highlight-header mt-4">Client Master</h2>
        <form method="post">
            <!-- Form fields -->
            <div class="form-row">
                <label for="firstName" class="required">First Name</label>
                <input type="text" id="firstName" name="FirstName" placeholder="Enter First Name" required>
            </div>

            <div class="form-row">
                <label for="middleName">Middle Name</label>
                <input type="text" id="middleName" name="MiddleName" placeholder="Enter Middle Name">
            </div>

            <div class="form-row">
                <label for="lastName" class="required">Last Name</label>
                <input type="text" id="lastName" name="LastName" placeholder="Enter Last Name" required>
            </div>

            <div class="form-row">
                <label for="suffix">Suffix</label>
                <select id="suffix" name="Suffix">
                    <option value="">Select Suffix</option>
                    <option value="Jr">Jr</option>
                    <option value="Sr">Sr</option>
                    <option value="III">III</option>
                </select>
            </div>

            <div class="form-row">
                <label for="nameDataQuality">Name Data Quality</label>
                <select id="nameDataQuality" name="NameDataQuality">
                    <option value="">Select Name Data Quality</option>
                    <option value="Verified">Verified</option>
                    <option value="Unverified">Unverified</option>
                </select>
            </div>

            <div class="form-row">
                <label for="ssn" class="required">SSN</label>
                <input type="text" id="ssn" name="SSN" placeholder="___-__-____" required>
            </div>

            <div class="form-row">
                <label for="ssnDataQuality" class="required">SSN Data Quality</label>
                <select id="ssnDataQuality" name="SSNDataQuality">
                    <option value="">Select SSN Data Quality</option>
                    <option value="Verified">Verified</option>
                    <option value="Unverified">Unverified</option>
                </select>
            </div>

            <div class="form-row">
                <label for="dob" class="required">Date of Birth</label>
                <input type="date" id="dob" name="DateOfBirth" required>
            </div>

            <div class="form-row">
                <label for="dobType">Date of Birth Type</label>
                <select id="dobType" name="DateOfBirthType">
                    <option value="">Select Date of Birth Type</option>
                    <option value="Estimated">Estimated</option>
                    <option value="Exact">Exact</option>
                </select>
            </div>

            <div class="form-row">
                <label for="race" class="required">Race and Ethnicity</label>
                <select id="race" name="Race" required>
                    <option value="">Select Race</option>
                    <option value="Asian">Asian</option>
                    <option value="Black">Black</option>
                    <option value="Hispanic">Hispanic</option>
                </select>
            </div>

            <div class="form-row">
                <label for="gender" class="required">Gender</label>
                <select id="gender" name="Gender" required>
                    <option value="">Select Gender</option>
                    <option value="Male">Male</option>
                    <option value="Female">Female</option>
                    <option value="Other">Other</option>
                </select>
            </div>

            <div class="form-row">
                <label for="contactPhone">Contact Phone</label>
                <input type="tel" id="contactPhone" name="ContactPhone" placeholder="(___)-___-____">
            </div>

            <div class="form-row">
                <label for="sexualOrientation">Sexual Orientation</label>
                <input type="text" id="sexualOrientation" name="SexualOrientation" placeholder="Enter Sexual Orientation">
            </div>

            <!-- Buttons -->
            <div class="btn-group">
                <button type="submit" class="btn btn-primary btn-custom">Save</button>
                <button type="button" class="btn btn-secondary btn-custom">Exit</button>
            </div>
        </form>
    </div>



<!-- Bootstrap & FontAwesome -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
